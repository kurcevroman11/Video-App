# ===== JWT =====
# Должен совпадать с JWT_SECRET в gateway-service (для валидации access-токена).
JWT_SECRET=change-me-jwt-secret

# ===== room-service (gRPC) =====
ROOM_SERVICE_HOST=localhost
ROOM_SERVICE_PORT=50051